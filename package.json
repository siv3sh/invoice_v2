{
  "name": "activus-invoice-management-system",
  "version": "1.0.0",
  "description": "Complete invoicing tool with FastAPI backend and React frontend",
  "scripts": {
    "build": "./build.sh",
    "start": "./start.sh",
    "dev": "./start.sh",
    "deploy": "./deploy.sh",
    "backend": "cd backend && uvicorn server:app --reload",
    "frontend": "cd frontend && npm run start",
    "frontend-yarn": "cd frontend && yarn start",
    "install": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install --legacy-peer-deps --force",
    "install-yarn": "cd backend && pip install -r requirements.txt && cd ../frontend && yarn install",
    "concurrent": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "keywords": [
    "invoicing",
    "fastapi",
    "react",
    "mongodb",
    "pdf-processing"
  ],
  "author": "Activus",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/siv3sh/invoice_v2.git"
  }
}